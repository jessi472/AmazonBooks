@*//Amazon Books Project Start: February 11th, 2022*@


@{
    ViewBag.Title = "Home Page";
}
@model BookViewModel

<h1>Amazon Books</h1>

@foreach (Book b in Model.Books)
{
<div class="card card-outline-primary m-1 p-1">
    <div class="bg-faded p-1">
        <h4>@b.Title<span style="float:right; font-size:medium">$@b.Price</span></h4>
    </div>
    @*<div class="card-text p-1">@p.Description</div> class="badge badge-pill badge-primary"*@
</div>

    <div page-model="@Model.PageInfo" page-action="Index" page-classes-enabled="true"
         page-class="btn" page-class-normal="btn-outline-dark"
         page-class-selected="btn-primary" class="btn-group pull-right m-1">
    </div>
    <ul>
        <li>Author: @b.Author</li>
        <li>Publisher: @b.Publisher</li>
        <li>ISBN: @b.Isbn</li>
        <li>Classification: @b.Classification</li>
        <li>Category: @b.Category</li>
        <li>Page Count: @b.PageCount</li>

    </ul>

    <br />


}

@*<div page-number="@Model.PageInfo" page-action="Index" page-classes-enabled="true"
          page-class="btn" page-class-normal="btn-outline-dark"
          page-class-selected="btn-primary" class="btn-group pull-right m-1"></div>*@
<div page-number="@Model.PageInfo" page-action="Index" page-classes-enabled="true" page-class="btn" page-class-normal="btn-outline-dark" page-class-selected="btn-primary" 
     class="btn-group pull-right m-1"></div>

